/**
 * Placeholder for a future Hero3D component (Three.js / React Three Fiber).
 * Keep this as a separate slot so Hero.tsx stays usable as a fallback.
 */
export default function HeroVisual() {
  return (
    <div className="hero-visual" aria-hidden="true">
      <div className="hero-visual-grid" />
    </div>
  )
}
